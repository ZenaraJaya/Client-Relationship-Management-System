rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Locked down by default for production safety.
      // Server-side writes via service account (Laravel backend) are not
      // affected by these client SDK rules.
      allow read, write: if false;
    }
  }
}
